query GetOrganization($id: String, $slug: String) {
  organizationCollection(id: $id, slug: $slug) {
    id
    name
    slug
    plan
    email
    logo
    members {
      roles
      user {
        id
        name
        email
        status
      }
    }
  }
}
