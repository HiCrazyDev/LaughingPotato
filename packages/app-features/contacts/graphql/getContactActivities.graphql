query GetContactActivities($id: String) {
  activitiesCollection(contactId: $id) {
    id
    user {
      id
      name
      avatar
      status
    }
    type
    data
    date
    createdAt
    updatedAt
  }
}
